export interface StatusOption {
  id: string;
  name: string;
}

export interface Timeline {
  id: string;
  name: string;
  description: string;
  startDate: string;
  endDate: string;
  // status: string;
}
